<div class="container">
  <h2>Bonuri fiscale</h2>
  <% if !@tickets.empty? %>
    <div class="container">
      <table class="table table-tickets ">
        <% i = 0 %>
        <% @tickets.each do |t| %>
          <% if t.citizen == current_citizen %>
            <%i = i+1%>
            <tr class='clickable-row' data-href = <%= ticket_path(t) %>>
              <td><%= i %></td>
              <td cursor="pointer"><%= t.winning_id %></td>
              <td>
                <%= link_to 'Detalii', t %>
                <%= link_to 'Șterge', t, method: :delete, data: { confirm: 'Sunteți sigur?' } %>
              </td>
            </tr>
          <% end %>
        <% end %>
      </table>
    </div>
  <%else%>
    <h4 style="color:#ff9933;">Nu sunt tichete înregistrate</h4>
  <% end %>

  <script type="text/javascript">
    jQuery(document).ready(function($) {
      $(".clickable-row").click(function() {
        window.document.location = $(this).data("href");
      });
    });
  </script>
</div>
